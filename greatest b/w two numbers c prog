#include<stdio.h>
int main()
{
    int a,b;
    printf("enter any two values\n");
    scanf("%d,%d",&a,&b);
    if (a>b)
    printf("the greatest number is %d\n",a);
    else
    printf("the greatest number is %d",b);
    return 0;
}
